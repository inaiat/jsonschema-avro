export enum AvroTypes {
  Null = 'null',
  String = 'string',
  Boolean = 'boolean',
  Integer = 'int',
  Number = 'float',
  Long = 'long',
  Record = 'record',
  Array = 'array',
  Enum = 'enum',
}
